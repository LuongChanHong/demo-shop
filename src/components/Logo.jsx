import React from "react";
import "../css/logo.css";

const Logo = () => {
  return (
    <section className="logo__container d-flex align-items-center justify-content-center rounded-circle">
      <div>LOGO</div>
    </section>
  );
};

export default Logo;
